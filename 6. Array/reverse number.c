#include <stdio.h>
int main()
{
    int arr[100], r[100], i,j, n;
    printf("Enter how many numbers: ");
    scanf("%d", &n);

    printf("Numbers: ");
    for(i=0; i<n; i++)
    {
        scanf("%d", &arr[i]);
    }
        printf("\nReverse of the numbers: ");
        for(i=n-1; i>=0; i--)
        {
            *
        }

    return 0;
}

